#pragma once // include guard

/**
* @brief Generates a random 32-bit integer
* @param seed A seed for the random number. If none is provided, seed = 0.
* @return Random 32-bit integer
*/
int arand(int seed);
